<x-app-layout>
<div class="container mx-auto py-8">
    <div class="max-w-3xl mx-auto bg-white shadow rounded-lg p-6">
        <div class="flex justify-between items-start">
            <div>
                <h2 class="text-2xl font-semibold">Prescription #{{ $prescription->id }}</h2>
                <div class="text-sm text-gray-600">Created: {{ $prescription->created_at->format('Y-m-d H:i') }}</div>
            </div>
            <div class="text-right">
                <div class="font-medium">{{ $prescription->doctor->name ?? '-' }}</div>
                <div class="text-sm text-gray-500">{{ $prescription->doctor->specialization ?? '' }}</div>
            </div>
        </div>

        <hr class="my-4">

        <div>
            <h3 class="font-medium">Patient</h3>
            <div>{{ $prescription->patient->name }}</div>
            @if($prescription->patient->phone)<div class="text-sm text-gray-500">Phone: {{ $prescription->patient->phone }}</div>@endif
            @if($prescription->patient->email)<div class="text-sm text-gray-500">Email: {{ $prescription->patient->email }}</div>@endif
        </div>

        <div class="mt-4">
            <h3 class="font-medium">Problem</h3>
            <div class="text-gray-700">{{ $prescription->problem_description ?? '-' }}</div>
        </div>

        <div class="mt-4">
            <h3 class="font-medium">Medicines</h3>
            @if($prescription->medicines->isEmpty())
                <div class="text-gray-500">No medicines</div>
            @else
                <ul class="list-disc list-inside">
                    @foreach($prescription->medicines as $m)
                        <li>
                            <strong>{{ $m->name }}</strong>
                            @if($m->pivot->duration) — Duration: {{ $m->pivot->duration }} @endif
                            @if($m->pivot->times_per_day) — Times/day: {{ $m->pivot->times_per_day }} @endif
                        </li>
                    @endforeach
                </ul>
            @endif
        </div>

        <div class="mt-4">
            <h3 class="font-medium">Tests</h3>
            @if($prescription->tests->isEmpty())
                <div class="text-gray-500">No tests</div>
            @else
                <ul class="list-disc list-inside">
                    @foreach($prescription->tests as $t)
                        <li>{{ $t->name }} @if($t->price) — ৳{{ $t->price }} @endif</li>
                    @endforeach
                </ul>
            @endif
        </div>

        <div class="mt-4">
            <h3 class="font-medium">Doctor Advice</h3>
            <div class="text-gray-700">{{ $prescription->doctor_advice ?? '-' }}</div>
        </div>

        <div class="mt-6">
            <a href="{{ route('prescriptions.index') }}" class="text-blue-600 hover:underline">← Back to prescriptions</a>
        </div>
    </div>
</div>
</x-app-layout>
